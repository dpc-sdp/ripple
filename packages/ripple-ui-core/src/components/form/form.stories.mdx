import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplForm from './form.vue'
import { RplFormsVariants } from './constants'
import { a11yStoryCheck } from './../../../.storybook/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplForm },
  setup() {
    return { args }
  },
  template: '<RplForm v-bind="args" />'
})

<Meta title='Components/Forms' component={RplForm} />

# Forms

<ArgsTable of={RplForm} />

<Canvas>
  <Story
    name='Default'
    play={a11yStoryCheck}
    argTypes={{
      schema: {
        defaultValue: [
      {
          $cmp: 'FormKit',
          props: {
            type: 'text',
            label: 'Income',
            help: 'Select your household income',
            suffixIcon: 'search'
          }
        },
        {
          $cmp: 'FormKit',
          props: {
            type: 'select',
            label: 'Marital status',
            options: [
              'Married',
              'Single',
              'Divorced',
              'Engaged'
            ]
          }
        }
        ]
      }
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
