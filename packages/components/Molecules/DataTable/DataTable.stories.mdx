import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import RplDataTable from './DataTable.vue'

<Meta title="Molecules/DataTable" />

# DataTable
Please see the [README](https://github.com/dpc-sdp/ripple/blob/master/packages/components/Molecules/DataTable/README.md) for more information about how to install and setup the component.

export const TemplateDataTable = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RplDataTable },
  template: `
    <rpl-data-table
      :caption='caption'
      is-row-oriented
      :items='items'
    />
  `
});

## Fixed row header

<Canvas>
  <Story
    name="FixedRowHeader"
    args={{
      caption: 'When viewed on mobile, each column will be assigned to its corresponding header which is in first row.',
      items: [
        ['Fruit', 'Vegetable', 'Elements'],
        ['Apple', 'Potato', 'Zinc'],
        ['Orange', 'Broccoli', 'Copper'],
        ['Banana', 'Pumpkin', 'Iron']
      ]
    }}
  >
    {TemplateDataTable.bind({})}
  </Story>
</Canvas>

## Fixed column header

export const TemplateDataTableColOriented = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RplDataTable },
  template: `
    <rpl-data-table
      :caption='caption'
      v-bind:is-row-oriented="false"
      :items='items'
    />
  `
});

<Canvas>
  <Story
    name="FixedColumnHeader"
    args={{
      caption: 'When viewed on mobile, first columns will be the fixed title for the following row columns.',
      items: [
        ['Fruit', 'Apple', 'Orange', 'Banana'],
        ['Vegetable', 'Potato', 'Broccoli', 'Pumpkin'],
        ['Elements', 'Zinc', 'Copper', 'Iron'],
      ]
    }}
  >
    {TemplateDataTableColOriented.bind({})}
  </Story>
</Canvas>

## Stackable columns

export const TemplateDataTableStackable = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RplDataTable },
  template: `
    <rpl-data-table
      :caption='caption'
      :is-stackable-columns="isStackableColumns"
      :items='items'
    />
  `
});

<Canvas>
  <Story
    name="StackableColumns"
    args={{
      caption: 'On mobile layout, each columns will be stack up together as one set.',
      isStackableColumns: true,
      items: [
        ['Fruit', 'Vegetable', 'Elements'],
        ['Apple', 'Potato', 'Zinc'],
        ['Orange', 'Broccoli', 'Copper'],
        ['Banana', 'Pumpkin', 'Iron']
      ]
    }}
  >
    {TemplateDataTableStackable.bind({})}
  </Story>
</Canvas>

<ArgsTable of={RplDataTable} />

