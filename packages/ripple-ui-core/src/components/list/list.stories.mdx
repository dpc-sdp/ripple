import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplList from './list.vue'
import { a11yStoryCheck } from './../../../.storybook/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplList },
  setup() {
    return { args }
  },
  template: '<RplList v-bind="args" />'
})

<Meta title='WIP/Components/List' component={RplList} />

# List

<ArgsTable of={RplList} />

<Canvas>
  <Story
    name='Default'
    play={a11yStoryCheck}
    args={{
      items: [
        {
          title: '1'
        }
      ]
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
