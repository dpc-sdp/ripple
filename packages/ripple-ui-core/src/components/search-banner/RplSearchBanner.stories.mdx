import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplSearchBanner from './RplSearchBanner.vue'
import { a11yStoryCheck } from './../../../stories/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplSearchBanner },
  setup() {
    return {
      args
    }
  },
  template: `<RplSearchBanner v-bind="args" />`
})

<Meta
  title='Core/Navigation/Search banner'
  component={RplSearchBanner}
  parameters={{
    layout: 'fullscreen',
  }}
  args={{
    title: 'Search banner',
    intro: 'A neat little search banner.'
  }}
/>

# Search banner

<ArgsTable of={RplSearchBanner} />

## Default

Simple banner with no image

<Canvas>
  <Story
    name='Default'
    play={a11yStoryCheck}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>

## Image

Banner with image to the top (mobile) and right (desktop)

<Canvas>
  <Story
    name='Image'
    play={a11yStoryCheck}
    args={{
      image: {
        src: '/img/image-landscape-m.jpg'
      }
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
