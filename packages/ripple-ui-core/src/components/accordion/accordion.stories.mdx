import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplAccordion from './accordion.vue'
import { RplAccordionThemes } from './constants'
import { a11yStoryCheck } from './../../../.storybook/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplAccordion },
  setup() {
    return { args }
  },
  template: '<RplAccordion v-bind="args" />'
})

<Meta title='Components/Accordion' component={RplAccordion} />

# Accordion

<ArgsTable of={RplAccordion} />

<Canvas>
  <Story
    name='Default'
    play={a11yStoryCheck}
    argTypes={{
      theme: {
        control: { type: 'select' },
        options: RplAccordionThemes,
        defaultValue: RplAccordionThemes[0]
      }
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
