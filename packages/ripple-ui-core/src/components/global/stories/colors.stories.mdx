import { Meta, Canvas, Story } from '@storybook/addon-docs'
import sbTokenTable from './../../../../.storybook/components/token-table.vue'
import sbTokenTableRow from './../../../../.storybook/components/token-row.vue'
import sbTokenTableCol from './../../../../.storybook/components/token-col.vue'
import sbTokenTableSwatch from './../../../../.storybook/components/token-swatch.vue'
import { getColorSwatches, default as rplColors } from './colors'

export const Template = (args) => ({
  components: { sbTokenTable, sbTokenTableRow, sbTokenTableCol, sbTokenTableSwatch },
  setup() {
    return {
      args
    }
  },
  template: `
    <sbTokenTable :columns="args.columns" >
      <sbTokenTableRow v-for="item in args.items" :key="item">
        <sbTokenTableCol>{{item.value}}</sbTokenTableCol>
        <sbTokenTableCol collapse width="50px"><sbTokenTableSwatch :size="24" :color="'var('+item.var+')'"></sbTokenTableSwatch></sbTokenTableCol>
        <sbTokenTableCol expand>{{item.var}}</sbTokenTableCol>
      </sbTokenTableRow>
    </sbTokenTable>
  `
});

# Colors

<Meta
  title='Base Styles/Colors'
  parameters={{
    viewMode: 'docs',
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/jtsOPNk1k4cWnumzkZgclD/Ripple-2.0-PoC?node-id=1916%3A101782',
    }
  }}
/>

## Core

<Canvas columns={2}>
  <Story
    name="Primary"
    args={{
      columns: ['Primary'],
      items: getColorSwatches(rplColors, 'core.primary')
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Accent"
    args={{
      columns: ['Accent'],
      items: getColorSwatches(rplColors, 'core.accent')
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Neutral

<Canvas>
  <Story
    name="Neutral"
    args={{
      columns: ['Neutrals'],
      items: getColorSwatches(rplColors, 'neutral')
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Semantic

<Canvas>
  <Story
    name="Info"
    args={{
      columns: ['Info'],
      items: getColorSwatches(rplColors, 'semantic.info')
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Success"
    args={{
      columns: ['Success'],
      items: getColorSwatches(rplColors, 'semantic.success')
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Warning"
    args={{
      columns: ['Warning'],
      items: getColorSwatches(rplColors, 'semantic.warning')
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Error"
    args={{
      columns: ['Error'],
      items: getColorSwatches(rplColors, 'semantic.error')
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
