import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import RplTabs from './Tabs.vue'

<Meta title="Molecules/Tabs" />

# Tabs
Please see the [README](https://github.com/dpc-sdp/ripple/blob/master/packages/components/Molecules/Tabs/README.md) for more information about how to install and setup the component.

## Basic example

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RplTabs },
  methods: {
    switchTab (e) {
      this.activeTab = e
    }   
  },
  template: `<rpl-tabs :tabs="tabs" :activeTab="activeTab" @rpl-tab-switch="switchTab" />`
});

<Canvas>
  <Story
    name="Default"
    args={{
      tabs: [
        {                                                                                                                                                                                                                                                                                                                                                                                       
          label: 'Map view', 
          key: 'map',
          icon: 'map_pin'
        },
        { 
          label: 'List view', 
          key: 'list',
          icon: 'list'
        }
      ],
      activeTab: 'map'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>


<ArgsTable of={RplTabs} />

