# Ripple with custom Drupal content type example

This is a example for adding a custom content type(Drupal content type).

This example assume we have a custom content type with name `movie` in Tide(Drupal).
