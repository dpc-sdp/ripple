import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import RplDataTable from './DataTable.vue'

<Meta title="Molecules/DataTable" />

# DataTable
Please see the [README](https://github.com/dpc-sdp/ripple/blob/master/packages/components/Molecules/DataTable/README.md) for more information about how to install and setup the component.

export const TemplateDataTable = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RplDataTable },
  template: `
    <rpl-data-table
      is-row-oriented
      :items="items"
    />
  `
});

## Row oriented

<Canvas>
  <Story
    name="RowOriented"
    args={{
      items: {
        0: ['Band', 'Singer', 'Inception', 'Label'],
        1: ['Napalm Death', 'Barney Greenway', '1981', 'Century Media'],
        2: ['Carcass', 'Jeff Walker', '1985', 'Earache'],
        3: ['Extreme Noise Terror', 'Dean Jones', '1985', 'Candlelight'],
        4: ['Discordance Axis', 'Jon Chang', '1992', 'Hydrahead']
      }
    }}
  >
    {TemplateDataTable.bind({})}
  </Story>
</Canvas>

## Column oriented

export const TemplateDataTableColOriented = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RplDataTable },
  template: `
    <rpl-data-table
      v-bind:is-row-oriented="false"
      :items="items"
    />
  `
});

<Canvas>
  <Story
    name="ColOriented"
    args={{
      items: {
        0: ['Band', 'Maroon 5', 'U2', 'Paramore'],
        1: ['Singer', 'Adam Levine', 'John Doe', 'Jane Doe'],
        2: ['Inception', '2000', '1985', '2004'],
        3: ['Label', 'Century Media', 'Candlelight', 'Century Media'],
      }
    }}
  >
    {TemplateDataTableColOriented.bind({})}
  </Story>
</Canvas>

<ArgsTable of={RplDataTable} />

