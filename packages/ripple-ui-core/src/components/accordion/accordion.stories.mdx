import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplAccordion from './accordion.vue'
import { RplAccordionItems } from './constants'
import { a11yStoryCheck } from './../../../.storybook/interactions.js'

export const Template = (args) => ({
  components: { RplAccordion },
  setup() {
    return {
      args,
      RplAccordionItems
    }
  },
  template: `
    <RplAccordion
      v-bind="args"
      :items="RplAccordionItems"
    />
  `
})

<Meta
  title='Components/Accordion' component={RplAccordion}
/>

# Accordion

<ArgsTable of={RplAccordion} />

<Canvas>
  <Story
    name='Accordion'
    play={a11yStoryCheck} // Is this needed?
  >
    {Template.bind()}
  </Story>
</Canvas>
