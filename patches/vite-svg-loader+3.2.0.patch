diff --git a/node_modules/vite-svg-loader/index.js b/node_modules/vite-svg-loader/index.js
index f0e7f82..43c038e 100644
--- a/node_modules/vite-svg-loader/index.js
+++ b/node_modules/vite-svg-loader/index.js
@@ -26,14 +26,14 @@ module.exports = function svgLoader (options = {}) {
 
       let svg = await fs.readFile(path, 'utf-8')
 
-      if (importType === 'raw') {
-        return `export default ${JSON.stringify(svg)}`
-      }
-
       if (svgo !== false) {
         svg = optimizeSvg(svg, svgoConfig).data
       }
 
+      if (importType === 'raw') {
+        return `export default ${JSON.stringify(svg)}`
+      }
+
       const { code } = compileTemplate({
         id: JSON.stringify(id),
         source: svg,
