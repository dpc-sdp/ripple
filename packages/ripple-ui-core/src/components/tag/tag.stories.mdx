import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplTag from './tag.vue'
import { RplTagVariants } from './constants'
import { a11yStoryCheck } from './../../../stories/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplTag },
  setup() {
    return { args }
  },
  template: '<div class="sb-container"><RplTag v-bind="args" /></div>'
})

<Meta title='Core/Containers/Tag' component={RplTag} />

# Tag

<ArgsTable of={RplTag} />

## Default

<Canvas>
  <Story
    name='Default'
    play={a11yStoryCheck}
    argTypes={{
      variant: {
        control: { type: 'select' },
        options: RplTagVariants,
        defaultValue: 'default'
      }
    }}
    args={{
      label: 'Tag'
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>

## Neutral

<Canvas>
  <Story
    name='Neutral'
    play={a11yStoryCheck}
    argTypes={{
      variant: {
        control: { type: 'select' },
        options: RplTagVariants,
        defaultValue: 'neutral'
      }
    }}
    args={{
      label: 'Tag'
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
