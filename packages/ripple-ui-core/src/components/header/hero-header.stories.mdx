import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplHeroHeader from './hero-header.vue'
import { RplHeaderThemes } from './constants'
import { a11yStoryCheck } from './../../../stories/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplHeroHeader },
  setup() {
    return { args }
  },
  template: '<RplHeroHeader v-bind="args" />'
})

<Meta
  title='WIP/Components/Header'
  component={RplHeroHeader}
  argTypes={{
    title: {
      control: 'text'
    },
    content: {
      control: 'text'
    },
    variant: {
      control: { type: 'radio' },
      options: RplHeaderThemes
    },
  }}
  args={{
    title: 'The main hero title',
    content: 'In do dolore dolore sint ipsum est est, commodo ex laborum Lorem ut deserunt dolore ullamco.'
  }}
/>

# Header

<ArgsTable of={RplHeroHeader} />

<Canvas>
  <Story
    name='Hero'
    play={a11yStoryCheck}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>

## Example / With Background

This is an example the hero header with optional background

<Canvas>
  <Story
    name='Hero/Background'
    play={a11yStoryCheck}
    args={{
      background: {
        src: 'https://images.unsplash.com/photo-1588587786374-ab73fa246083?w=1680&q=80',
        srcSet: 'https://images.unsplash.com/photo-1588587786374-ab73fa246083?w=640&q=80 640w, https://images.unsplash.com/photo-1588587786374-ab73fa246083?w=960&q=80 960w',
        sizes: '(min-width: 640px) 640px, (min-width: 960px) 960px'
      }
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>

## Example / With Actions

This is an example the hero header with optional actions i.e. cta

<Canvas>
  <Story
    name='Hero/Actions'
    play={a11yStoryCheck}
    args={{
      primaryAction: { text: 'Primary action', url: '#primary' },
      secondaryAction: { text: 'Secondary action', url: '#secondary' },
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>

## Example / With Links

This is an example the hero header with optional links

<Canvas>
  <Story
    name='Hero/Links'
    play={a11yStoryCheck}
    args={{
      links: {
        heading: 'Want to know more about..',
        more: { text: 'See more', url: '#more' },
        items: [
          {text: 'First links', url: '#first'},
          {text: 'Second link', url: '#second'},
          {text: 'A third link', url: '#third'},
          {text: 'And a forth link', url: '#forth'}
        ]
      }
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>

## Example / With Logo

This is an example the hero header with optional links

<Canvas>
  <Story
    name='Hero/Logo'
    play={a11yStoryCheck}
    args={{
      logo: svgPlaceholder({ width: 150, height: 50, bgColor: '#ad9', fgColor: '#efc' })
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>


## Example / Reversed

This is an example the hero header using the reversed theme

<Canvas>
  <Story
    name='Hero/Reversed'
    play={a11yStoryCheck}
    args={{
      theme: 'reverse'
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
