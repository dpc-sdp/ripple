import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplPageAction from './page-action.vue'
import RplDocument from '../document/document.vue'
import RplFile from '../file/file.vue'
import RplIcon from '../icon/icon.vue'
import { a11yStoryCheck } from './../../../stories/interactions.js'

export const PrintDocument = `<RplDocument url="https://vic.gov.au/#print">
  <template #icon>
    <RplIcon name="icon-print-lined" size="l" colour="default" />
  </template>
  <template #name>
    Print document
  </template>
</RplDocument>`

<Meta
  title='WIP/Components/Page action'
  component={RplPageAction}
  args={{
    name: 'The document title',
    url: 'https://vic.gov.au/',
    size: '1.5mb',
    extension: 'pdf',
    printDocument: PrintDocument
  }}
/>

# Page Action

<ArgsTable of={RplPageAction} />

<Canvas>
  <Story
    name='Default'
    play={a11yStoryCheck}
  >
    {(args) => {
      return {
        components: { RplPageAction, RplDocument, RplFile, RplIcon },
        setup() {
          return { args }
        },
        template: `
          <RplPageAction>
            <template #upper>
              ${args.printDocument}
            </template>
            <RplFile v-bind="args" />
            <RplFile v-bind="args" />
          </RplPageAction>
        `
      }
    }}
  </Story>
</Canvas>

## Example / With only documents

This is example shows the page action component with only documents.

<Canvas>
  <Story
    name='Only Documents'
    play={a11yStoryCheck}
  >
    {(args) => {
      return {
        components: { RplPageAction, RplDocument, RplFile },
        setup() {
          return { args }
        },
        template: `
          <RplPageAction>
            <RplFile v-bind="args" />
            <RplFile v-bind="args" />
            <RplFile v-bind="args" caption="A short description of the document" />
          </RplPageAction>
        `
      }
    }}
  </Story>
</Canvas>

## Example / With only print

This is example shows the page action component with only print.

<Canvas>
  <Story
    name='Only Print'
    play={a11yStoryCheck}
  >
  {(args) => {
    return {
        components: { RplPageAction, RplDocument, RplFile, RplIcon },
        template: `
          <RplPageAction>
            <template #upper>
              ${args.printDocument}
            </template>
          </RplPageAction>
        `
      }
    }}
  </Story>
</Canvas>
