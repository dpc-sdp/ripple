import {
  Canvas,
  Meta,
  Story
} from '@storybook/addon-docs'
import { ArgsTable } from '@storybook/addon-docs'
import RplTextLink from './TextLink.vue'
import RplIcon from '../icon/icon.vue'
import { within, userEvent } from '@storybook/testing-library'
import { eventCheck, a11yCheck, a11yStoryCheck } from './../../../.storybook/interactions.js'
const eventTestFunction = async ({ canvasElement }) => {
  const canvas = await within(canvasElement)
  const link = await canvas.getByRole('link')
  await a11yCheck(canvasElement)
  await eventCheck(link, 'rpl-text-link/click')
}

export const Template = (args) => ({
  components: { RplTextLink },
  setup() {
    return { args }
  },
  template: '<p class="sb-container">This para contains a <RplTextLink @click.prevent="args.onClick()" v-bind="args">link</RplTextLink> in the middle.</p>'
})

<Meta
  title='Components/Atoms/Text link'
  parameters={{
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/jtsOPNk1k4cWnumzkZgclD/Ripple-2.0-PoC?node-id=2415%3A122102',
    }
  }}
  component={{RplTextLink}}
  argTypes={{
    theme: {
      control: { type: 'select' },
      options: ['primary', 'accent', 'neutral'],
      defaultValue: 'primary'
    },
    onClick: { action: 'clicked' }
  }}
/>

# Text link states

<ArgsTable of={RplTextLink} />

## Text link / Default

<Canvas>
  <Story
    play={a11yStoryCheck}
    name='Default'
  >
    {Template.bind()}
  </Story>
</Canvas>

## Text link / Hover

<Canvas>
  <Story
    name='Default/Hover'
    play={a11yStoryCheck}
    parameters={{ pseudo: { hover: true } }}
  >
    {Template.bind()}
  </Story>
</Canvas>

## Text link / Active

<Canvas>
  <Story
    name='Default/Active'
    play={a11yStoryCheck}
    parameters={{ pseudo: { active: true } }}
  >
    {Template.bind()}
  </Story>
</Canvas>

## Text link / Focus

<Canvas>
  <Story
    name='Default/Focus'
    play={a11yStoryCheck}
    parameters={{ pseudo: { focus: true } }}
  >
    {Template.bind()}
  </Story>
</Canvas>

## Text link / Visited

<Canvas>
  <Story
    name='Default/Visited'
    play={a11yStoryCheck}
    parameters={{ pseudo: { visited: true } }}
  >
    {Template.bind()}
  </Story>
</Canvas>

## Text link / Visited Hover

<Canvas>
  <Story
    name='Default/VisitedHover'
    play={a11yStoryCheck}
    parameters={{ pseudo: { hover: true, visited: true } }}
  >
    {Template.bind()}
  </Story>
</Canvas>

## Text link / Inactive

<Canvas>
  <Story
    name='Default/Inactive'
    play={a11yStoryCheck}
    args={{
      inactive: true
    }}
  >
    {Template.bind()}
  </Story>
</Canvas>

# Text link examples

## Text link / Content examples

<Canvas>
  <Story name="Examples/Content/Accent" play={a11yStoryCheck}>
    {(args) => {
      return {
        components: { RplTextLink },
        setup() { return { args } },
        template: '<p class="sb-container">This para contains a <RplTextLink>voilà</RplTextLink> in the middle.</p>'
      }
    }}
  </Story>
  <Story name="Examples/Content/Emoji" play={a11yStoryCheck}>
    {(args) => {
      return {
        components: { RplTextLink },
        setup() { return { args } },
        template: '<p class="sb-container">This para contains a <RplTextLink>😍</RplTextLink> in the middle.</p>'
      }
    }}
  </Story>
  <Story name="Examples/Content/Target" play={a11yStoryCheck}>
    {(args) => {
      return {
        components: { RplTextLink },
        setup() { return { args } },
        template: `<p class="sb-container">This para contains a <RplTextLink target="_blank">link that opens in a new window</RplTextLink> in the middle.</p>`
      }
    }}
  </Story>
</Canvas>

## Text link / Component examples

<Canvas>
  <Story name="Examples/Component/Icon" play={a11yStoryCheck}>
    {(args) => {
      return {
        components: { RplTextLink, RplIcon },
        setup() { return { args } },
        template: `<p class="sb-container">This para contains a <RplTextLink><RplIcon name="icon-download" title="download icon"></RplIcon></RplTextLink> in the middle.</p>`
      }
    }}
  </Story>
</Canvas>

## Text link / Eventbus

<Canvas>
  <Story
    name="Examples/Eventbus"
    play={eventTestFunction}
  >
    {(args) => {
      return {
        components: { RplTextLink },
        setup() { return { args } },
        template: `<p class="sb-container">This para contains an <RplTextLink @click.prevent="args.onClick()">event-tracked link</RplTextLink> in the middle.</p>`
      }
    }}
  </Story>
</Canvas>
