import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplSocialShare from './social-share.vue'
import { RplIconGroups } from './../icon/constants'
import { a11yStoryCheck } from './../../../.storybook/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplSocialShare },
  setup() {
    return { args }
  },
  template: '<RplSocialShare v-bind="args" />'
})

<Meta title='Components/Navigation/Social share' component={RplSocialShare} />

# Social share

<ArgsTable of={RplSocialShare} />

<Canvas>
  <Story
    name='Social share'
    play={a11yStoryCheck}
    args={{
      items: RplIconGroups.social.map(soc => {
        return {
          label: soc.charAt(5).toUpperCase() + soc.substring(6),
          url: 'https://' + soc.charAt(5),
          icon: soc
        }
      })
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
