import { Canvas, Meta, Story } from '@storybook/addon-docs'
import RplIcon from './icon.vue'

<Meta
  title='Example/Icons'
  component={RplIcon}
  argTypes={{
    name: { table: { disable: true } }
  }}
/>

# Core icons

Core icons are delivered through an SVG sprite, capturing all of the most commonly used icons and referenced via `<use>`.

```
<svg class="rpl-icon rpl-icon--download" aria-hidden="true">
  <use xlink:href="#rpl-icon--download"></use>
</svg>
```

<Canvas>
  <Story name='Download'>
    {() => {
      return {
        components: { RplIcon },
        template: `<RplIcon name="download"></RplIcon>`
      }
    }}
  </Story>
  <Story name='Home'>
    {() => {
      return {
        components: { RplIcon },
        template: `<RplIcon name="home"></RplIcon>`
      }
    }}
  </Story>
</Canvas>

# Custom icons

Custom icons are rendered directly inline.

```
<svg class="rpl-icon rpl-icon--facebook" aria-hidden="true">
  <path>
    ...
  </path>
</svg>
```

<Canvas>
  <Story name='Facebook'>
    {() => {
      return {
        components: { RplIcon },
        template: `<RplIcon name="facebook"></RplIcon>`
      }
    }}
  </Story>
</Canvas>
