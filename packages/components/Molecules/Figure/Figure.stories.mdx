import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import RplFigure from './index.vue'

<Meta title="Molecules/Figure" />

# Figure
Please see the [README](https://github.com/dpc-sdp/ripple/blob/master/packages/components/Molecules/Figure/README.md) for more information about how to install and setup the component.

## Basic example

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RplFigure },
  template: `<rpl-figure :image="image" :caption="caption" :source="source" />`
});

<Canvas>
  <Story
    name="Figure"
    args={{
      image: {
        src: 'https://via.placeholder.com/800x400',
        alt: 'A generic square placeholder image.'
      },
      caption: '1.2 Caption to go here. This should be restricted in characters.',
      source: ''
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={RplFigure} />

