import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs'
import Trackable from './index.vue'

<Meta
  title='Example/Trackable'
  argTypes={{ onClick: {} }}
  component={Trackable}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Trackable },
  template: `<trackable />`
})

<Canvas>
  <Story name='Trackable'>{Template.bind({})}</Story>
</Canvas>

<ArgsTable of={Trackable} />
