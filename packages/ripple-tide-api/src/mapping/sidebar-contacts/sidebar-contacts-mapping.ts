import { TideApiResponse } from '../../../types';
import type { TideContact } from './sidebar-contacts-mapping-types';

export const map = (src: TideApiResponse): TideContact[] => {
  return []
}

export const includes = [

];
