import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplSocialShare from './social-share.vue'
import { a11yStoryCheck } from './../../../.storybook/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplSocialShare },
  setup() {
    return { args }
  },
  template: '<RplSocialShare v-bind="args" />'
})

<Meta title='Components/Navigation/Social share' component={RplSocialShare} />

# Social share

<ArgsTable of={RplSocialShare} />

<Canvas>
  <Story
    name='Social share'
    play={a11yStoryCheck}
    args={{
      networks: ['facebook', 'twitter', 'linkedin'],
      page: {
        title: 'Sample page title',
        url: 'https://sample.page.link'
      }
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
