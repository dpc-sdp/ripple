import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplPageLinks from './page-links.vue'
import { a11yStoryCheck } from './../../../stories/interactions.js'

export const SingleTemplate = (args) => ({
  components: { RplPageLinks },
  setup() {
    return { args }
  },
  template: '<div class="sb-demo-container"><RplPageLinks v-bind="args" /></div>'
})

<Meta
  title='WIP/Components/Page links'
  component={RplPageLinks}
/>

# Page links

<ArgsTable of={RplPageLinks} />

<Canvas>
  <Story
    name='Page links'
    play={a11yStoryCheck}
    args={{
      prev: { text: 'Check out this page', url: '#page' },
      next: { text: 'Another page to look at', url: '#another-page' },
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>

## Example / With "Page counts"

This is an example page links showing supplied "Page count""

<Canvas>
  <Story
    name='Example/Count'
    play={a11yStoryCheck}
    args={{
      prev: { text: '2 of 5', url: '#page' },
      next: { text: '4 of 5', url: '#another-page' },
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>

# Example / Last page

This is an example of the page links on the final linkable page

<Canvas>
  <Story
    name='Example/Final page'
    play={a11yStoryCheck}
    args={{
      prev: { text: 'The title of a page', url: '#page' }
    }}
  >
    {SingleTemplate.bind()}
  </Story>
</Canvas>
