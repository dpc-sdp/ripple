import {
  Canvas,
  Meta,
  Story
} from '@storybook/addon-docs'

import fixture from '../../../examples/nuxt-app/test/fixtures/publication/sample-publication.json'
import MockPage from '../../ripple-ui-core/stories/components/mock-page.vue'
import TidePublicationPage from './index.vue'

export const PublicationTemplate = (args) => ({
  components: {
    MockPage
  },
  setup() {
    return { args }
  },
  template: `<MockPage v-bind="args"></MockPage>`
})

<Meta
  title='Publication/Template'
  component={TidePublicationPage}
  argTypes={{
    componentName: { table: { disable: true } },
    pageComponent: { table: { disable: true } },
    aboveHeader: { table: { disable: true } },
    primaryNav: { table: { disable: true } },
    sidebar: { table: { disable: true } },
    footer: { table: { disable: true } },
    default: { table: { disable: true } }
  }}
  args={{
    componentName: 'TidePublication',
    pageComponent: TidePublicationPage,
    url: 'https://www.vic.gov.au',
    breadcrumbs: {
      items: [
        { label: 'Home', url: '/' },
        { label: 'Publication', url: '/publication' }
      ]
    },
    site: {},
    page: fixture
  }}
  parameters={{
    layout: 'fullscreen'
  }}
/>

# Publication

<Canvas>
  <Story name='Template'>
    {PublicationTemplate.bind()}
  </Story>
</Canvas>
