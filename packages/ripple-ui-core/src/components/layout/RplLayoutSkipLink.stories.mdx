import {
  Canvas,
  Meta,
  Story,
  ArgsTable
} from '@storybook/addon-docs'
import RplLayoutSkipLink from './RplLayoutSkipLink.vue'
import { a11yStoryCheck } from './../../../stories/interactions.js'
import sbSpacer from './../../../stories/components/spacer.vue'

export const FullWidthTemplate = (args) => ({
  components: { RplLayoutSkipLink },
  setup() {
    return { args }
  },
  template: `
    <RplLayoutSkipLink>Skip to main content</RplLayoutSkipLink>
    <p class="rpl-type-p rpl-u-padding-5">Use tab key to see skip link</p>
  `
})

<Meta
  title='Core/Layout/Skip links'
  component={RplLayoutSkipLink}
  parameters={{
    layout: 'fullscreen'
  }}
/>

# Layout

<ArgsTable of={RplLayoutSkipLink} />

<Canvas>
  <Story
    name='stand alone'
    play={a11yStoryCheck}
  >
    {FullWidthTemplate.bind()}
  </Story>
</Canvas>
