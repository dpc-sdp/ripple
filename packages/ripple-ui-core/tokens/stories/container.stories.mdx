import { Meta, Canvas, Story } from '@storybook/addon-docs'

export const Template = (args) => ({
  components: {},
  setup() {
    return {
    }
  },
  template: `
  <div class="rpl-container">
    <span class="rpl-type-p">
      This container has a maximum width of 1200px and includes a margin to
      prevent content from touching the edges of the page.
    </span>
  </div>
  `
});

# Container

<Meta title='Base Styles/Container' parameters={{ viewMode: 'docs'}}/>

<Canvas>
  <Story name="Container" parameters={{
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/jtsOPNk1k4cWnumzkZgclD/Ripple-2.0-PoC?node-id=1286%3A17345',
    }
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
